extends layouts/main.pug

block content
    .form-container
        form(action=routes.editPhoto(photo.id), method="post")
            textarea(placeholder="Description", name="description")=photo.description
            input(type="submit", value="Update")
        a.form-container__link.form-container__link--delete(href=routes.deletePhoto(photo.id)) Delete